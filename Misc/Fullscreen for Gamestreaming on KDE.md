# Fullscreen for Gamestreaming on KDE

In the window-specific settings, add  a new rule "Keep above all => True". Select **exact match** for the **window class** and set the value to match the string `microsoft-edge`.