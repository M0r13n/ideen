# Full Docker cleanup

Clean up as much as possible:

`docker system prune -a`

## Clean up unused images

`docker image prune`

## Clean up containers

`docker container prune`

## Clean up volumes

`docker volume prune`